# QEMU

```bash
./run.sh
```

```bash
Ctrl + A, C # Switch to QEMU console
Ctrl + A, X # Exit QEMU
Ctrl + A, H # Help
Ctrl + A, S # Save disk data back to file (if -snapshot)
Ctrl + A, T # Toggle console timestamps
Ctrl + A, B # Send break (magic sysrq)
Ctrl + A, Ctrl + A # Send Ctrl + A
```

